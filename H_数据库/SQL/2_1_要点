PRIMARY KEY		主键
AUTO_INCREMENT	自增，MYSQL中有
NOT NULL		非空
UNIQUE			唯一
CONSTRAINT		外键
	语法：
		CONSTRAINT 外键名 FOREIGN KEY(column1) REFERENCES table2(key);

数据库字符集
	显示字符集：SET CHARACTER_SET_RESULTS = gbk;
使用关键字作为表名，使用`将表名包围起来即可

常见函数
	日期相关
		ADDDATE(E,E)
		ADDTIME(E,E)
		CURDATE()
		CURTIME()
		CURRENT_DATE()
		CURRENT_TIME()
		CURRENT_TIMESTAMP()
		DATE(E)