迭代器模式:
	提供一种方法顺序访问一个聚合对象中的各个元素，而又不暴露其内部的表示。

	意图：提供一种方法顺序访问一个聚合对象中各个元素, 而又无须暴露该对象的内部表示。

	实现：
	    定义迭代器接口，至少提供hasNext和next方法；
	    根据对象数据结构定义具体迭代器实现类，实现迭代方式的统一包装；
	    在数据对象内定义获取其迭代器的接口，其实就是new出一个迭代器，并将自己维护的数据结构传给迭代器。
	    注：多使用内部类方式实现，这样就不用将自己维护的数据结构传给迭代器，因为内部类可以自己访问该数据结构。